<h2>注文内容確認</h2>
<p>ご注文の内容を確認してください</p>
<h4>購入する商品</h4>
<table class="table">
  <tr class="thead-light">
    <th>商品名</th>
    <th>単価</th>
    <th>数量</th>
    <th>小計</th>
  </tr>
  <% @orders_details.each do |item| %>
    <tr>
      <td><%= item.product.name %></td>
      <td><%= item.price %>円</td>
      <td><%= item.amount %></td>
      <td><%= item.price * item.amount %>円</td>
    </tr>
  <% end %>
  <tr>
    <td>配送料</td>
    <td></td>
    <td></td>
    <td><%= @shipping_fee %>円</td>
  </tr>
  <tr>
    <td>合計</td>
    <td></td>
    <td></td>
    <td><%= @total %>円</td>
  </tr>
</table>
<h4>配送先郵便番号</h4>
<ul>
  <li class="list-unstyled ml-1"><%= @order.postal_code %></li>
</ul>
<h4>配送先住所</h4>
<ul>
  <li class="list-unstyled ml-1"><%= @order.address %></li>
</ul>
<h4>配送先宛名</h4>
<ul>
  <li class="list-unstyled ml-1"><%= @order.reciever %></li>
</ul>
<h4>お支払い方法</h4>
<ul>
  <li class="list-unstyled ml-1"><%= @order.payment %></li>
</ul>
  <div>
  <%= form_with model: @order, url: orders_path do |f|%>
    <%= f.hidden_field  :payment, value: @order.payment %>
    <%= f.hidden_field  :postal_code, value: @order.postal_code %>
    <%= f.hidden_field  :address, value: @order.address %>
    <%= f.hidden_field  :reciever, value: @order.reciever %>
    <%= link_to "カートに戻る", cart_items_path, class:"btn btn-outline-dark" %>
    <%= f.submit "次に進む", class:"btn btn-success"%>
  </div>
<% end %>