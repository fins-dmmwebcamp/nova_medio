<div>
  <h2>ユーザー情報詳細</h2>
  <span class="btn btn-outline-primary">いいねした商品</span>
  <span class="btn btn-outline-primary">レビュー一覧</span>
  <span class="btn btn-outline-primary">情報を編集する</span>
  <table>
    <tbody>
      <tr>
        <td>名前 :</td>
        <td><%= @customer.name_full %></td>
      </tr>
      <tr>
        <td>電話番号：</td>
        <td><%= @customer.phone_number %></td>
      </tr>
      <tr>
        <td>メールアドレス：</td>
        <td><%= @customer.email %></td>
      </tr>
      <tr>
        <td>郵便番号：</td>
        <td><%= @destination.postal_code %></td>
      </tr>
      <tr>
        <td>住所：</td>
        <td><%= @destination.address_prefecture %><%= @destination.address_city %><%= @destination.address_after %></td>
      </tr>
    </tbody>
  </table>
  <h3>お届け先一覧</h3>
  <table>
    <tbody>
    <% @destinations.each do |destination| %>
    <tr>
      <td><%= link_to '編集', destination_edit_path(destination) %></td>
      <td><%= destination.reciever %></td>
      <td><%= destination.address_prefecture %><%= destination.address_city %><%= destination.address_after %></td>
    </tr>
    <% end %>
    </tbody>
  </table>
  <span class="btn btn-outline-primary">退会する</span>
</div>